use crate::constants::PrimitivesType::Int;
use phf::phf_map;
use crate::constants::PrimitivesType;

pub enum List {
    TypedList,
    UntypedList,
}

// static PRIMITIVES_LIST_TYPE: phf::Map<u8, &'static str> = phf_map! {
// };
